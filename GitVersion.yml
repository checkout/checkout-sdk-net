mode: ContinuousDelivery
ignore:
  sha: []
branches:
  develop:
    increment: Patch
    tag: unstable
  release:
    mode: ContinuousDelivery
    tag: rc
    increment: Patch
    prevent-increment-of-merged-branch-version: true
